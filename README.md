This is the gh-pages branch for fplot.

Currently, the github pages for fplot is just a mirror of its wiki.

Eventually the github pages for fplot will be more independent.


The makefile was copied over from cl-freelock. And the preamble for the tex file in this branch was also copied over from cl-freelock.